Wrap Labels- https://www.flerlagetwins.com/2024/04/wrap-text.html 

REPLACE(
REGEXP_REPLACE([Dim], "(\S{" + STR([Parameter. Split Length]) + "}|[\s\S]{1," + STR([Parameter. Split Length]) + "}(?!\S))", "$1" + CHAR(13) + CHAR(10))
 , CHAR(13) + CHAR(10) + " ", CHAR(13) + CHAR(10)
)

